# UWB定位服务器依赖

# Core dependencies
numpy>=1.24.0  # Kalman filter matrix operations

# Optional dependencies
# protobuf>=4.0.0  # If using protobuf format for messages

# Testing dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
